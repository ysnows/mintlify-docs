---
title: "Developing Extensions"
description: "Create a TypeScript command or Local API route with the Enconvo SDK."
---

An Enconvo plugin is an ESM package with metadata in `package.json`, commands under `src/`, and optional API routes under `src/api/`. Commands run through Enconvo's extension runtime; a plugin command is a default-exported function, not a class implementing a `Command` interface.

## Minimal project

Use Node.js compatible with your installed `@enconvo/api` package (the current SDK declares Node.js 18 or later), a package manager, and Enconvo on your Mac.

```text
hello-enconvo/
├── package.json
├── tsconfig.json
├── src/
│   ├── hello.ts
│   └── api/
│       └── echo.ts
└── assets/
```

Create `package.json`:

```json
{
  "$schema": "https://enconvo.com/schemas/extension.json",
  "name": "hello_enconvo",
  "version": "1.0.0",
  "title": "Hello Enconvo",
  "description": "A small example command and API.",
  "author": "Your Name",
  "type": "module",
  "categories": ["Tools"],
  "commands": [
    {
      "name": "hello",
      "title": "Say Hello",
      "description": "Return a greeting for the input text.",
      "commandType": "command",
      "mode": "no-view"
    }
  ],
  "scripts": {
    "build": "enconvo plugin build",
    "dev": "enconvo plugin build --dev"
  }
}
```

Install the SDK and TypeScript:

```sh
npm install @enconvo/api
npm install --save-dev typescript
```

Create `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "skipLibCheck": true,
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src"]
}
```

## Add the command

`commands[].name` matches the source filename. Put this in `src/hello.ts`:

```typescript
import { EnconvoResponse, type RequestOptions } from "@enconvo/api";

export default async function main(request: Request) {
  const options = await request.json() as RequestOptions;
  const name = String(options.input_text || "World");
  return EnconvoResponse.text(`Hello, ${name}!`);
}
```

The command key is `hello_enconvo|hello`. Runtime preferences and request parameters are merged into the request body.

## Add a Local API route

Files in `src/api/` are discovered during the build and need no `commands[]` entry. Put this in `src/api/echo.ts`:

```typescript
interface EchoParams {
  /** Text to return. @required */
  text: string;
}

/** Return the supplied text. */
export default async function main(request: Request) {
  const params = await request.json() as EchoParams;
  if (typeof params.text !== "string") {
    return Response.json({ error: "text must be a string" }, { status: 400 });
  }
  return Response.json({ text: params.text });
}
```

This becomes `hello_enconvo/echo`. A directory such as `src/api/items/list.ts` becomes `hello_enconvo/items/list`. Use the installed [Local API schemas](/advanced/cli-local-api) for exact request shapes.

## Build and inspect

```sh
npm run build
npm run dev
npx enconvo plugin generate-schema
```

Build from the plugin directory. Review the build output and then find **Say Hello** in Enconvo. Development mode watches files for rebuilds. API schemas are generated into the plugin's `skills/` directory.

Test a greeting and invalid API input before sharing the plugin. A successful build alone does not test native operations or every provider call.

## Package and publish

Add an icon and the metadata required by the current package schema, document prerequisites, and check that no credentials or private assets are bundled. Inspect `npx enconvo plugin publish --help` before publishing; the command performs authentication and publishes to the store.

For SDK building blocks, see [Extension API Reference](/extensions/extension-api-reference). For reusable multi-step processes that do not need a plugin, use [Workflows](/workflows/introduction).
